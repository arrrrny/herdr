# GitHub usernames with Herdr maintainer authority, one per line.
#
# This repository is the fork `arrrrny/herdr`. `arrrrny` is the fork owner and
# maintainer, so fork-local agent/maintainer actions (opening PRs against the
# fork, merging into the fork, etc.) are authorized here. The canonical
# repository `herdrdev/herdr` is maintained upstream by `ogulcancelik` and
# `kangal-bot`, who are NOT maintainers of this fork.
arrrrny
